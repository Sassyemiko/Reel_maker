<!-- templates/web.html -->
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Reels_maker - AI Video Generator</title>
<style>
:root {
--bg-color: #0f0f13;
--card-bg: #1a1a24;
--text-color: #e0e0e0;
--accent-color: #8DB600;
--accent-hover: #7CAF00;
--border-color: #333;
--input-bg: #252530;
}
body {
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
background-color: var(--bg-color);
color: var(--text-color);
margin: 0;
padding: 0;
min-height: 100vh;
display: flex;
flex-direction: column;
align-items: center;
}
.container {
width: 100%;
max-width: 900px;
padding: 40px 20px;
}
header {
text-align: center;
margin-bottom: 40px;
}
h1 {
font-size: 32px;
margin: 0 0 10px 0;
background: linear-gradient(90deg, #8DB600, #00d2ff);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
}
.card {
background-color: var(--card-bg);
border-radius: 12px;
padding: 30px;
margin-bottom: 20px;
border: 1px solid var(--border-color);
box-shadow: 0 4px 20px rgba(0,0,0,0.3);
}
.card-title {
font-size: 18px;
font-weight: 600;
margin-bottom: 20px;
color: #fff;
display: flex;
align-items: center;
gap: 10px;
}
.toggle-container {
display: flex;
gap: 20px;
margin-bottom: 20px;
}
.toggle-btn {
flex: 1;
padding: 12px;
background: var(--input-bg);
border: 1px solid var(--border-color);
border-radius: 8px;
color: var(--text-color);
cursor: pointer;
transition: all 0.3s ease;
text-align: center;
font-weight: 500;
}
.toggle-btn.active {
background: var(--accent-color);
color: #000;
border-color: var(--accent-color);
}
.form-group {
margin-bottom: 20px;
}
label {
display: block;
margin-bottom: 8px;
font-size: 14px;
color: #aaa;
}
input[type="text"], input[type="number"], textarea, select {
width: 100%;
padding: 12px;
background: var(--input-bg);
border: 1px solid var(--border-color);
border-radius: 8px;
color: #fff;
font-size: 14px;
box-sizing: border-box;
font-family: inherit;
}
input:focus, textarea:focus, select:focus {
outline: none;
border-color: var(--accent-color);
}
textarea {
min-height: 150px;
resize: vertical;
}
.grid-select {
display: grid;
grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
gap: 15px;
margin-top: 10px;
}
.grid-item {
background: var(--input-bg);
border: 2px solid var(--border-color);
border-radius: 8px;
padding: 15px;
cursor: pointer;
transition: all 0.2s ease;
text-align: center;
font-size: 13px;
}
.grid-item:hover {
border-color: #666;
}
.grid-item.selected {
border-color: var(--accent-color);
background: rgba(141, 182, 0, 0.1);
}
.generate-btn {
width: 100%;
padding: 16px;
background: linear-gradient(90deg, #8DB600, #00d2ff);
border: none;
border-radius: 8px;
color: #000;
font-size: 16px;
font-weight: 700;
cursor: pointer;
transition: opacity 0.3s ease;
margin-top: 20px;
}
.generate-btn:hover {
opacity: 0.9;
}
.generate-btn:disabled {
opacity: 0.5;
cursor: not-allowed;
}
.hidden {
display: none;
}
#result-container {
margin-top: 30px;
text-align: center;
}
video {
width: 100%;
max-width: 400px;
border-radius: 12px;
border: 2px solid var(--border-color);
margin-top: 20px;
}
.loader {
border: 4px solid var(--input-bg);
border-top: 4px solid var(--accent-color);
border-radius: 50%;
width: 40px;
height: 40px;
animation: spin 1s linear infinite;
margin: 20px auto;
}
@keyframes spin {
0% { transform: rotate(0deg); }
100% { transform: rotate(360deg); }
}
.status-text {
text-align: center;
color: #aaa;
margin-top: 10px;
}
.file-upload-wrapper {
position: relative;
overflow: hidden;
display: inline-block;
width: 100%;
}
.file-upload-wrapper input[type=file] {
font-size: 100px;
position: absolute;
left: 0;
top: 0;
opacity: 0;
cursor: pointer;
}
.file-upload-btn {
background: var(--input-bg);
border: 2px dashed var(--border-color);
border-radius: 8px;
padding: 30px;
text-align: center;
color: #aaa;
cursor: pointer;
transition: all 0.3s ease;
}
.file-upload-btn:hover {
border-color: var(--accent-color);
color: var(--accent-color);
}
.duration-selector {
display: flex;
gap: 10px;
margin-top: 10px;
}
.duration-btn {
flex: 1;
padding: 10px;
background: var(--input-bg);
border: 1px solid var(--border-color);
border-radius: 6px;
color: var(--text-color);
cursor: pointer;
transition: all 0.3s ease;
text-align: center;
}
.duration-btn.active {
background: var(--accent-color);
color: #000;
border-color: var(--accent-color);
}
</style>
</head>
<body>
<div class="container">
<header>
<h1>🎬 Reels_maker</h1>
<p style="color:#888">AI-Powered Viral Video Generator</p>
</header>
<form id="video-form">
<!-- Input Mode Selection -->
<div class="card">
<div class="card-title">📝 Content Source</div>
<div class="toggle-container">
<div class="toggle-btn active" onclick="setMode('url')" id="btn-url">Reddit URL</div>
<div class="toggle-btn" onclick="setMode('manual')" id="btn-manual">Manual Story</div>
<div class="toggle-btn" onclick="setMode('mp4')" id="btn-mp4">MP4 Upload</div>
</div>

<!-- Mode 1: Reddit URL -->
<div id="url-input-group" class="form-group">
<label>Reddit Thread URL</label>
<input type="text" id="reddit-url" placeholder="https://www.reddit.com/r/confessions/comments/1s5t9fs/...">
</div>

<!-- Mode 2: Manual Story -->
<div id="manual-input-group" class="form-group hidden">
<label>Story Title</label>
<input type="text" id="story-title" placeholder="Enter a catchy title">
<label style="margin-top:15px">Story Body</label>
<textarea id="story-body" placeholder="Write your story here..."></textarea>
</div>

<!-- Mode 3: MP4 Upload -->
<div id="mp4-input-group" class="form-group hidden">
<label>Upload Your Video (MP4)</label>
<div class="file-upload-wrapper">
<div class="file-upload-btn" id="file-upload-label">
📁 Click to Select Video or Drag & Drop
<br><small style="color:#666">Max 100MB • MP4 format</small>
</div>
<input type="file" id="video-upload" accept="video/mp4">
</div>
<div id="file-name" style="margin-top:10px;color:#8DB600"></div>

<label style="margin-top:20px">Video Title (for Reddit Card)</label>
<input type="text" id="mp4-title" placeholder="Enter title for Reddit card">

<label style="margin-top:20px">Card Duration (seconds)</label>
<div class="duration-selector">
<div class="duration-btn" onclick="setDuration(5, this)" data-duration="5">5 sec</div>
<div class="duration-btn active" onclick="setDuration(7, this)" data-duration="7">7 sec</div>
<div class="duration-btn" onclick="setDuration('custom', this)" data-duration="custom">Custom</div>
</div>
<input type="number" id="card-duration" value="7" min="0" max="10" style="margin-top:10px" placeholder="Enter duration (0-10 seconds)">
<small style="color:#666">Card will appear for first N seconds, then fade out</small>
</div>
</div>

<!-- Voice Selection (Hidden for Mode 3) -->
<div class="card" id="voice-card">
<div class="card-title">🎤 Voice Selection</div>
<select id="voice-select">
<option value="" disabled selected>Loading voices...</option>
</select>
</div>

<!-- Background Selection (Hidden for Mode 3) -->
<div class="card" id="background-card">
<div class="card-title">🎮 Background Video</div>
<div class="grid-select" id="background-grid">
<div class="grid-item">Loading...</div>
</div>
<input type="hidden" id="selected-background">
</div>

<!-- Character Overlay - REMOVED -->

<button type="submit" class="generate-btn" id="generate-btn">🚀 Generate Video</button>
</form>

<div id="result-container" class="hidden">
<div class="card">
<div class="card-title">✅ Generation Complete</div>
<div id="loader" class="loader hidden"></div>
<div id="status-text" class="status-text">Processing...</div>
<div id="video-output"></div>
</div>
</div>
</div>

<script>
let currentMode = 'url';
let availableBackgrounds = [];
let selectedDuration = 7;

// Initialize Options
window.addEventListener('load', () => {
fetchOptions();
});

const fetchOptions = async () => {
try {
const response = await fetch('/api/options');
const data = await response.json();

// Populate Voices
const voiceSelect = document.getElementById('voice-select');
voiceSelect.innerHTML = '';
data.voices.forEach((voice) => {
const option = document.createElement('option');
option.value = voice.id;
option.textContent = voice.name;
voiceSelect.appendChild(option);
});
voiceSelect.value = 'en-US-JennyNeural';

// Populate Backgrounds
availableBackgrounds = data.backgrounds;
renderBackgrounds();
} catch (error) {
console.error('Error loading options:', error);
}
};

const renderBackgrounds = () => {
const grid = document.getElementById('background-grid');
grid.innerHTML = '';
if (availableBackgrounds.length === 0) {
grid.innerHTML = '<div class="grid-item">No videos found in reels/videos folder</div>';
return;
}
availableBackgrounds.forEach((bg, index) => {
const item = document.createElement('div');
item.className = 'grid-item';
item.textContent = bg.name;
item.onclick = () => selectBackground(index, item);
grid.appendChild(item);
});
if (availableBackgrounds.length > 0) {
selectBackground(0, grid.children[0]);
}
};

const selectBackground = (index, element) => {
document.querySelectorAll('.grid-item').forEach((el) => el.classList.remove('selected'));
element.classList.add('selected');
document.getElementById('selected-background').value = availableBackgrounds[index].path;
};

const setMode = (mode) => {
currentMode = mode;
document.getElementById('btn-url').classList.toggle('active', mode === 'url');
document.getElementById('btn-manual').classList.toggle('active', mode === 'manual');
document.getElementById('btn-mp4').classList.toggle('active', mode === 'mp4');

// Show/hide input groups
document.getElementById('url-input-group').classList.toggle('hidden', mode !== 'url');
document.getElementById('manual-input-group').classList.toggle('hidden', mode !== 'manual');
document.getElementById('mp4-input-group').classList.toggle('hidden', mode !== 'mp4');

// Hide Voice & Background for Mode 3
const voiceCard = document.getElementById('voice-card');
const backgroundCard = document.getElementById('background-card');
if (mode === 'mp4') {
voiceCard.classList.add('hidden');
backgroundCard.classList.add('hidden');
} else {
voiceCard.classList.remove('hidden');
backgroundCard.classList.remove('hidden');
}
};

const setDuration = (duration, element) => {
selectedDuration = duration;
document.querySelectorAll('.duration-btn').forEach((btn) => btn.classList.remove('active'));
element.classList.add('active');

const customInput = document.getElementById('card-duration');
if (duration === 'custom') {
customInput.disabled = false;
customInput.focus();
} else {
customInput.value = duration;
customInput.disabled = true;
}
};

// File upload handler
const fileInput = document.getElementById('video-upload');
const fileName = document.getElementById('file-name');
const uploadLabel = document.getElementById('file-upload-label');

fileInput.addEventListener('change', (e) => {
if (e.target.files.length > 0) {
fileName.textContent = '✅ Selected: ' + e.target.files[0].name;
uploadLabel.style.borderColor = '#8DB600';
uploadLabel.style.color = '#8DB600';
}
});

// Form submit
document.getElementById('video-form').addEventListener('submit', async (e) => {
e.preventDefault();
const btn = document.getElementById('generate-btn');
const resultContainer = document.getElementById('result-container');
const loader = document.getElementById('loader');
const statusText = document.getElementById('status-text');
const videoOutput = document.getElementById('video-output');

btn.disabled = true;
btn.textContent = '⏳ Generating...';
resultContainer.classList.remove('hidden');
loader.classList.remove('hidden');
videoOutput.innerHTML = '';
statusText.textContent = 'Initializing generation pipeline...';

const payload = {
mode: currentMode,
voice: document.getElementById('voice-select').value,
background: document.getElementById('selected-background').value,
character: 'none'
};

if (currentMode === 'url') {
payload.url = document.getElementById('reddit-url').value;
} else if (currentMode === 'manual') {
payload.title = document.getElementById('story-title').value;
payload.story = document.getElementById('story-body').value;
} else if (currentMode === 'mp4') {
// Handle file upload
const file = fileInput.files[0];
if (!file) {
alert('Please select a video file!');
btn.disabled = false;
btn.textContent = '🚀 Generate Video';
return;
}

const formData = new FormData();
formData.append('video', file);
formData.append('title', document.getElementById('mp4-title').value);
formData.append('duration', document.getElementById('card-duration').value);
formData.append('mode', 'mp4');

try {
const response = await fetch('/generate-quick', {
method: 'POST',
body: formData
});
const data = await response.json();
loader.classList.add('hidden');

if (response.ok && data.success) {
statusText.textContent = 'Video ready for download!';
const timestamp = new Date().getTime();
videoOutput.innerHTML = `
<video controls autoplay>
<source src="${data.video_url}?t=${timestamp}" type="video/mp4">
Your browser does not support the video tag.
</video>
<br>
<a href="${data.video_url}?t=${timestamp}" download="reels_maker_video.mp4" style="display:inline-block;margin-top:15px;padding:10px 20px;background:#8DB600;color:#000;text-decoration:none;border-radius:5px;font-weight:bold">Download MP4</a>
`;
} else {
statusText.textContent = 'Generation Failed';
videoOutput.innerHTML = `<p style="color:#ff6b6b">${data.error || 'Unknown error occurred'}</p>`;
}
} catch (error) {
loader.classList.add('hidden');
statusText.textContent = 'Connection Error';
videoOutput.innerHTML = `<p style="color:#ff6b6b">${error.message}</p>`;
} finally {
btn.disabled = false;
btn.textContent = '🚀 Generate Video';
}
return;
}

try {
const response = await fetch('/generate', {
method: 'POST',
headers: { 'Content-Type': 'application/json' },
body: JSON.stringify(payload)
});
const data = await response.json();
loader.classList.add('hidden');

if (response.ok && data.success) {
statusText.textContent = 'Video ready for download!';
const timestamp = new Date().getTime();
videoOutput.innerHTML = `
<video controls autoplay>
<source src="${data.video_url}?t=${timestamp}" type="video/mp4">
Your browser does not support the video tag.
</video>
<br>
<a href="${data.video_url}?t=${timestamp}" download="reels_maker_video.mp4" style="display:inline-block;margin-top:15px;padding:10px 20px;background:#8DB600;color:#000;text-decoration:none;border-radius:5px;font-weight:bold">Download MP4</a>
`;
} else {
statusText.textContent = 'Generation Failed';
videoOutput.innerHTML = `<p style="color:#ff6b6b">${data.error || 'Unknown error occurred'}</p>`;
}
} catch (error) {
loader.classList.add('hidden');
statusText.textContent = 'Connection Error';
videoOutput.innerHTML = `<p style="color:#ff6b6b">${error.message}</p>`;
} finally {
btn.disabled = false;
btn.textContent = '🚀 Generate Video';
}
});
</script>
</body>
</html>
